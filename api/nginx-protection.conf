# Nginx protection snippet. Include this in the matching server/location scope.

location ~ ^/api/(config\.php|init\.php|waf\.php)$ {
    deny all;
    return 403;
}

location ^~ /api/waf_rules/ {
    deny all;
    return 403;
}

location ^~ /sensitive_words/ {
    deny all;
    return 403;
}
